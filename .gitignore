node_modules
dist
dist-ssr

# Build info from `tsc -b`
*.tsbuildinfo

# Local env
.env
.env.local
.env.*.local

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
*.log

# Scratch. Everything under public/ (photos, logo, icons, share card) is committed on purpose:
# it is the site's content, not build output.
.work
